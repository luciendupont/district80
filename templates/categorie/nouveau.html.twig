{% extends "base.html.twig" %}
{% block title %}nouvelle categorie{% endblock %}
{% block body %}
<div class="container">
    <h1 class="my-3">Creer la categorie</h1>
    {{ form_start(form) }}
    {{ form_row(form.libelle, {'attr': {'class': 'mt-2'}}) }}
    {{ form_row(form.image, {'attr': {'class': 'mt-2'}}) }}
    {{ form_row(form.active, {'attr': {'class': 'mt-2'}}) }}
    <button type="submit" class="btn btn-dark mt-3">creer une catégorie</button>
    {{ form_end(form) }}
</div>
{% endblock %}