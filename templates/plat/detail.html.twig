{% extends 'base.html.twig' %}

{% block title %}plat
{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row align-items-center">
        <div class="col">
            <div id="fond" class="p-5">
                <form action="" class="mt-4">
                    <input type="search" name="q" placeholder="recheche">
                    <input type="submit" value="rechecher">
                </form>
        </div>
    </div>
</div>
{% block article %}
<div class="container">
    <div class="row">
        <div class="col-4">
            <div class="card border-dark mb-3 mt-4">
                <div class="card-header">{{plat.libelle}}</div>
                <div class="card-body">
                    <img src="/images_the_district/food/{{plat.image}}" width="200">
                    <h4 class="card-title">
                        <p class="card-text">{{plat.description}}</p>
                        <p class="card-text">{{plat.prix}}€</p>
                        <a href="{{path('cart_add', {id:plat.id})}}" class="btn btn-dark">ajouter au panier</a>
                    </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% endblock %}