{% extends 'base.html.twig' %}

{% block title %}restaurant le district{% endblock %}

{% block body %}

<div class="container fluid">
    <div class="row">
        <div class="column">
            <img src="images_the_district/the_district_brand/logo_transparent.png" width="500">
            <form class="d-flex">
                <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
              </form>
</div>
    </div>
</div>

       
{% block article %}
<div class="container">
    <div class="row">
        <h1> catégories</h1>

        <div class="col-4">
            <div class="card border-dark mb-3" style="max-width: 20rem;">
                <div class="card-header">{{cats.libelle}}</div>
                <div class="card-body">
                    <img src="images_the_district/category/{{cats.image}}" width="200">
                    <h4 class="card-title">
                        <p class="card-text"></p>
                        <a href="{{path("app_categorie")}}" class="btn btn-dark">categorie</a>
                </div>
            </div>
        </div>

        <h1>plats</h1>
        <div class="container">
            <div class="col-4">


                <div class="card border-dark mb-3" style="max-width: 20rem;">
                    <div class="card-header">{{plats.libelle}}</div>
                    <div class="card-body">
                        <img src="images_the_district/food/{{plats.image}}" width="200">
                        <h4 class="card-title">
                            <p class="card-text"></p>
                            <a href="{{path("app_plat")}}" class="btn btn-dark">plat</a>
                    </div>
                </div>
            </div>

        </div> {% endblock %}
{% endblock %}
